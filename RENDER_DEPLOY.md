# Деплой на Render.com

## Шаг 1: Подготовка

1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Подключите ваш GitHub репозиторий

## Шаг 2: Создание базы данных PostgreSQL

1. В Render Dashboard нажмите **New +** → **PostgreSQL**
2. Заполните:
   - **Name**: `movieportal-db`
   - **Database**: `movies_db`
   - **User**: `movieportal`
   - **Region**: выберите ближайший
   - **Plan**: `Free`
3. Нажмите **Create Database**
4. Дождитесь создания (2-3 минуты)
5. **Скопируйте Internal Database URL** - понадобится позже

## Шаг 3: Создание Web Service

1. Нажмите **New +** → **Web Service**
2. Выберите ваш репозиторий
3. Заполните:
   - **Name**: `movieportal`
   - **Region**: тот же, что и БД
   - **Branch**: `main`
   - **Root Directory**: оставьте пустым
   - **Language**: выберите `Docker`
   - **Dockerfile Path**: оставьте `./Dockerfile` (по умолчанию)
   - **Plan**: `Free`

## Шаг 4: Настройка переменных окружения

В разделе **Environment** добавьте:

```bash
APP_ENV=production
BASE_URL=https://ваш-сайт.onrender.com
DATABASE_URL=<вставьте Internal Database URL из шага 2>
```

**Формат DATABASE_URL:**
```
postgresql://user:password@host:port/database
```

## Шаг 5: Деплой

1. Нажмите **Create Web Service**
2. Render автоматически:
   - Клонирует репозиторий
   - Соберет проект
   - Запустит PHP сервер
   - Выполнит миграции БД автоматически (при первом подключении)

## Шаг 6: Инициализация БД (если нужно)

Если нужно загрузить начальные данные:

1. Откройте **Shell** в Render Dashboard
2. Выполните:
```bash
php scripts/init_admin.php
```

## Готово! 🎉

Ваш сайт будет доступен по адресу:
```
https://movieportal.onrender.com
```

## Проблемы?

### База данных не подключается
- Проверьте правильность DATABASE_URL
- Убедитесь, что используется Internal Database URL (не External)

### Миграции не выполняются
- Проверьте логи в Render Dashboard
- Убедитесь, что файлы sql/admin_setup.sql и другие доступны

### Яндекс Метрика не работает
- Обновите BASE_URL в переменных окружения
- Добавьте домен Render в список разрешенных в Яндекс Метрике

## Альтернатива: Автоматический деплой через render.yaml

Если в корне проекта есть `render.yaml`, Render автоматически настроит всё:

```bash
git add render.yaml
git commit -m "Add Render configuration"
git push
```

Затем в Render Dashboard выберите **Blueprint** при создании сервиса.

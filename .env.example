# Database Configuration
DB_HOST=postgres
DB_NAME=movies_db
DB_USER=postgres
DB_PASSWORD=postgres

# Application Settings
BASE_URL=http://localhost
APP_ENV=development

# Render.com Production Settings (для деплоя)
# DATABASE_URL будет автоматически предоставлен Render PostgreSQL
# Формат: postgresql://user:password@host:port/database

# Система авторизации для админ-панели MoviePortal

## 🔐 Обзор

Создана полная система авторизации для админ-панели с использованием логина и пароля. Система включает:

- **Безопасную авторизацию** с хешированием паролей
- **Защиту всех админ-страниц** от несанкционированного доступа
- **Красивую страницу входа** с современным дизайном
- **Функцию выхода** из системы
- **Отображение текущего пользователя** в шапке админ-панели

## 📁 Созданные файлы

### 1. База данных
- `admin_setup.sql` - SQL-скрипт для создания таблицы админов
- `public/init_admin.php` - PHP-скрипт для инициализации базы данных

### 2. Авторизация
- `public/admin/login.php` - Страница входа в админ-панель
- `public/admin/auth.php` - Система проверки авторизации

### 3. Стили
- Добавлены стили для страницы входа в `public/admin/admin-styles.css`

## 🚀 Инструкция по установке

### Шаг 1: Инициализация базы данных

Выполните один из вариантов:

**Вариант А: Через браузер (рекомендуется)**
```
http://localhost/init_admin.php
```

**Вариант Б: Через psql**
```bash
docker exec -i postgres_container psql -U postgres -d movie_portal < admin_setup.sql
```

### Шаг 2: Проверка работы

1. Перейдите на страницу входа: `http://localhost/admin/login.php`
2. Используйте тестовые данные:
   - **Логин:** `admin`
   - **Пароль:** `admin123`

### Шаг 3: Безопасность (важно!)

После успешной инициализации **удалите файл** `public/init_admin.php` из соображений безопасности.

## 🔧 Как это работает

### Защита страниц
Все админ-страницы теперь защищены:
- `index.php` - главная панель
- `movies.php` - управление фильмами  
- `directors.php` - управление режиссерами
- `genres.php` - управление жанрами

### Автоматическое перенаправление
- При попытке доступа без авторизации → перенаправление на `login.php`
- После успешного входа → перенаправление на `index.php`
- При выходе → перенаправление на `login.php`

### Сессии
Система использует PHP-сессии для хранения состояния авторизации:
- `$_SESSION['admin_logged_in']` - флаг авторизации
- `$_SESSION['admin_id']` - ID админа
- `$_SESSION['admin_username']` - имя пользователя

## 🎨 Интерфейс

### Страница входа
- Современный градиентный дизайн
- Адаптивная верстка
- Красивые анимации при наведении
- Информация о тестовых данных

### Админ-панель
- Приветствие пользователя в шапке
- Кнопка выхода на всех страницах
- Сохранен существующий дизайн

## 🔒 Безопасность

### Хеширование паролей
- Используется `password_hash()` с алгоритмом BCRYPT
- Проверка через `password_verify()`

### Защита от атак
- Экранирование HTML через `htmlspecialchars()`
- Подготовленные SQL-запросы (prepared statements)
- Проверка CSRF через сессии

### Рекомендации
1. **Смените пароль по умолчанию** после первого входа
2. **Используйте сложные пароли** для продакшена
3. **Настройте HTTPS** для защиты передачи данных
4. **Регулярно обновляйте пароли**

## 📱 Адаптивность

Система полностью адаптивна и корректно работает на:
- Десктопах
- Планшетах  
- Мобильных устройствах

## 🛠 Кастомизация

### Добавление новых админов
```sql
INSERT INTO admin_users (username, password_hash, email) 
VALUES ('новый_админ', '$2y$10$...', 'email@example.com');
```

### Изменение стилей
Все стили находятся в `public/admin/admin-styles.css` в секции "Стили для страницы входа".

## ❓ Возможные проблемы

### "Database connection failed"
- Проверьте настройки подключения в `config.php`
- Убедитесь, что PostgreSQL запущен

### "Неверный логин или пароль"
- Проверьте, что таблица `admin_users` создана
- Убедитесь, что тестовый админ добавлен

### Перенаправление не работает
- Проверьте, что сессии включены в PHP
- Убедитесь, что нет вывода до `header()`

## 🎯 Готово!

Система авторизации полностью настроена и готова к использованию. Все админ-страницы теперь защищены, а доступ возможен только после успешной авторизации.

# Презентация MoviePortal - Структура (5 минут)

## 📊 Слайд 1: Титульный (15 сек)
**Заголовок:** MoviePortal - Веб-каталог фильмов  
**Подзаголовок:** Полнофункциональная система управления базой фильмов

**Содержание:**
- Название проекта
- ФИО, группа
- Дата защиты

---

## 📊 Слайд 2: Цель и задачи проекта (30 сек)

**Цель:** Разработка веб-приложения для каталогизации и поиска фильмов

**Задачи:**
1. Создание базы данных фильмов с режиссерами и жанрами
2. Реализация системы поиска и фильтрации
3. Разработка административной панели
4. Интеграция OAuth авторизации (Telegram, VK, Yandex)
5. Оптимизация для SEO и производительности

---

## 📊 Слайд 3: Технологический стек (45 сек)

**Backend:**
- PHP 8.x (серверная логика)
- PostgreSQL (реляционная БД)
- PDO (безопасная работа с БД)

**Frontend:**
- HTML5/CSS3 (семантическая верстка)
- Vanilla JavaScript (динамический поиск)
- Адаптивный дизайн

**Инфраструктура:**
- Docker Compose (контейнеризация)
- Nginx (веб-сервер, reverse proxy)
- OAuth 2.0 (Telegram, VK, Yandex)

**Схема:**
```
┌─────────────┐
│   Nginx     │ ← HTTP запросы
└──────┬──────┘
       │
┌──────▼──────┐
│  PHP-FPM    │ ← Обработка PHP
└──────┬──────┘
       │
┌──────▼──────┐
│ PostgreSQL  │ ← Данные
└─────────────┘
```

---

## 📊 Слайд 4: Архитектура базы данных (60 сек)

**Структура БД (ER-диаграмма):**

```
movies (фильмы)                directors (режиссеры)
├── id                         ├── id
├── title                      ├── name
├── year                       └── bio
├── director_id ──────────────┐
├── rating                     │
└── poster_url                 │
                               │
movie_genres (связь М:М)       │
├── movie_id ──────────────────┘
└── genre_id ──────────────┐
                           │
genres (жанры)             │
├── id ────────────────────┘
└── name
```

**Ключевые решения:**
- Нормализация до 3НФ
- Foreign Keys для целостности
- Индексы на полях поиска
- Связь "многие ко многим" через промежуточную таблицу

**Код (init.sql):**
```sql
CREATE TABLE movies (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    year INTEGER,
    director_id INTEGER REFERENCES directors(id),
    rating DECIMAL(3,1)
);

CREATE INDEX idx_movies_title ON movies(title);
CREATE INDEX idx_movies_year ON movies(year);
```

---

## 📊 Слайд 5: Программная реализация - Backend (75 сек)

**1. Безопасная работа с БД (config.php):**
```php
// PDO с prepared statements против SQL-injection
$pdo = new PDO(
    "pgsql:host=$host;dbname=$dbname", 
    $user, 
    $password,
    [PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION]
);

// Параметризованные запросы
$stmt = $pdo->prepare("SELECT * FROM movies WHERE id = ?");
$stmt->execute([$id]);
```

**2. Динамический поиск (search.php):**
```php
// AJAX endpoint для поиска в реальном времени
$query = $_GET['q'] ?? '';
$stmt = $pdo->prepare("
    SELECT m.*, d.name AS director
    FROM movies m
    JOIN directors d ON m.director_id = d.id
    WHERE m.title ILIKE ? OR d.name ILIKE ?
    LIMIT 10
");
$stmt->execute(["%$query%", "%$query%"]);
echo json_encode($stmt->fetchAll());
```

**3. OAuth авторизация:**
```php
// Telegram OAuth callback
$auth_data = $_GET;
if (checkTelegramAuthorization($auth_data)) {
    $_SESSION['user'] = [
        'id' => $auth_data['id'],
        'name' => $auth_data['first_name']
    ];
}
```

---

## 📊 Слайд 6: Программная реализация - Frontend (90 сек)

**1. Живой поиск (search.js):**
```javascript
// Поиск с debounce для оптимизации
let searchTimeout;
searchInput.addEventListener('input', (e) => {
    clearTimeout(searchTimeout);
    searchTimeout = setTimeout(() => {
        fetch(`search.php?q=${e.target.value}`)
            .then(res => res.json())
            .then(data => displayResults(data));
    }, 300); // Задержка 300мс
});
```

**2. Динамическая загрузка (loader.js):**
```javascript
// Skeleton loader для UX
function showLoader() {
    container.innerHTML = `
        <div class="skeleton-card"></div>
        <div class="skeleton-card"></div>
    `;
}
```

**3. Адаптивность (CSS):**
```css
/* Mobile-first подход */
.movie-card {
    width: 100%;
}

@media (min-width: 768px) {
    .movie-card { width: 48%; }
}

@media (min-width: 1200px) {
    .movie-card { width: 23%; }
}
```

---

## 📊 Слайд 7: Административная панель (105 сек)

**Функционал:**
- ✅ CRUD операции (Create, Read, Update, Delete)
- ✅ Управление фильмами, режиссерами, жанрами
- ✅ Многофакторная авторизация (OAuth + пароль)
- ✅ Защита от несанкционированного доступа

**Код авторизации (admin/auth.php):**
```php
// Проверка прав доступа
session_start();
if (!isset($_SESSION['admin_logged_in'])) {
    header('Location: login.php');
    exit();
}

// Защита от CSRF
if ($_POST['token'] !== $_SESSION['csrf_token']) {
    die('Invalid CSRF token');
}
```

**Добавление фильма (admin/movies.php):**
```php
if ($_POST['action'] === 'add') {
    $stmt = $pdo->prepare("
        INSERT INTO movies (title, year, director_id, rating)
        VALUES (?, ?, ?, ?)
    ");
    $stmt->execute([
        $_POST['title'],
        $_POST['year'],
        $_POST['director_id'],
        $_POST['rating']
    ]);
}
```

---

## 📊 Слайд 8: Docker и DevOps (120 сек)

**docker-compose.yml:**
```yaml
services:
  nginx:
    build: ./nginx
    ports: ["80:80"]
    depends_on: [php]
    
  php:
    build: ./php
    volumes: ["./public:/var/www/html/public"]
    
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: movies_db
    volumes: ["./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql"]
```

**Преимущества:**
- 🚀 Быстрое развертывание (1 команда)
- 🔄 Изолированное окружение
- 📦 Воспроизводимость на любой машине
- 🛠️ Легкое обновление компонентов

**Команды:**
```bash
docker-compose up -d      # Запуск
docker-compose down       # Остановка
docker-compose logs       # Просмотр логов
```

---

## 📊 Слайд 9: SEO и оптимизация (135 сек)

**Реализованные улучшения:**

**1. SEO:**
- ✅ robots.txt - управление индексацией
- ✅ sitemap.xml - карта сайта (935 байт)
- ✅ Meta теги (title, description, keywords)
- ✅ Open Graph для соцсетей
- ✅ Семантическая разметка HTML5

**2. Производительность (nginx.conf):**
```nginx
# Gzip сжатие
gzip on;
gzip_types text/css application/javascript;

# Кэширование статики
location ~* \.(css|js|jpg|png)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

**3. UX:**
- ✅ Кастомная страница 404
- ✅ Favicon (SVG)
- ✅ Адаптивный дизайн (3 breakpoints)
- ✅ Skeleton loaders

---

## 📊 Слайд 10: Результаты и метрики (150 сек)

**Реализовано:**
- 📊 15+ страниц (главная, каталог, админка)
- 🗄️ 4 основные таблицы БД
- 🔐 3 метода OAuth авторизации
- 🔍 Real-time поиск
- 📱 Адаптивный дизайн (320px - 1920px)
- ⚡ Оптимизация производительности

**Файловая статистика:**
```
Backend (PHP):    20+ файлов
Frontend (JS):    4 файла
Стили (CSS):      8 файлов
SQL:              4 файла миграций
Docker:           3 контейнера
```

**Соответствие чек-листу:**
- ✅ Валидный HTML5/CSS3
- ✅ SEO оптимизация (85%)
- ✅ Безопасность (prepared statements, CSRF защита)
- ✅ Производительность (gzip, кэш)

**Техническая сложность:**
- Реляционная БД с нормализацией
- OAuth интеграция (3 провайдера)
- AJAX без фреймворков
- Docker orchestration

---

## 📊 Слайд 11: Демонстрация (165 сек)

**Скриншоты для вставки:**

1. **Главная страница** - карточки фильмов
2. **Страница фильма** - детальная информация
3. **Живой поиск** - результаты в реальном времени
4. **Админ-панель** - таблица управления
5. **Мобильная версия** - адаптив

**Live Demo (если возможно):**
```bash
# Запуск проекта
docker-compose up -d

# Открыть: http://localhost
```

**Код на GitHub:** (если есть)

---

## 📊 Слайд 12: Заключение (180 сек = 3 мин)

**Достигнутые цели:**
- ✅ Создана полнофункциональная система
- ✅ Реализована безопасная работа с БД
- ✅ Интегрированы современные технологии
- ✅ Оптимизировано для продакшена

**Возможности развития:**
- 🔄 REST API для мобильных приложений
- 🤖 Рекомендательная система (ML)
- 📊 Расширенная аналитика
- 💬 Система комментариев и отзывов
- 🎬 Интеграция с внешними API (IMDB, Kinopoisk)

**Спасибо за внимание!**  
**Готов ответить на вопросы** (2 минуты)

---

# 📝 Возможные вопросы и ответы

## Вопрос 1: "Почему выбрали PostgreSQL, а не MySQL?"
**Ответ:**
- PostgreSQL более строгий к типам данных (ACID compliance)
- Лучшая поддержка JSON для будущего расширения
- Мощные полнотекстовые индексы для поиска
- ILIKE для регистронезависимого поиска

## Вопрос 2: "Как обеспечивается безопасность?"
**Ответ:**
- PDO Prepared Statements против SQL-injection
- Password hashing (password_hash/verify)
- CSRF токены в формах
- Session-based авторизация
- OAuth 2.0 для внешней аутентификации
- Валидация всех входных данных

## Вопрос 3: "Почему без фреймворков (Laravel, Symfony)?"
**Ответ:**
- Глубокое понимание основ PHP
- Контроль над каждым компонентом
- Минимальные зависимости
- Демонстрация знания архитектуры
- Оптимизация производительности

## Вопрос 4: "Как реализован поиск в реальном времени?"
**Ответ:**
```javascript
// Debounce для снижения нагрузки
let timeout;
input.addEventListener('input', (e) => {
    clearTimeout(timeout);
    timeout = setTimeout(() => {
        fetch(`search.php?q=${e.target.value}`)
            .then(res => res.json())
            .then(displayResults);
    }, 300);
});
```

## Вопрос 5: "Как масштабируется приложение?"
**Ответ:**
- Docker позволяет легко добавлять контейнеры
- Nginx как reverse proxy для балансировки
- PostgreSQL поддерживает репликацию
- Кэширование статики на CDN
- Возможность добавления Redis для сессий

## Вопрос 6: "Как обрабатываются ошибки?"
**Ответ:**
```php
try {
    $stmt = $pdo->prepare($query);
    $stmt->execute($params);
} catch (PDOException $e) {
    error_log($e->getMessage());
    http_response_code(500);
    echo json_encode(['error' => 'Database error']);
}
```

## Вопрос 7: "Как тестировали приложение?"
**Ответ:**
- Ручное тестирование всех функций
- Проверка через W3C Validator
- PageSpeed Insights для производительности
- Тестирование на разных браузерах
- Проверка безопасности (SQL injection, XSS)

---

# 🎯 Рекомендации для презентации

## ✅ Что показывать:
1. **Код** - чистый, с комментариями
2. **Архитектуру** - диаграммы, схемы
3. **Результат** - скриншоты, live demo
4. **Метрики** - цифры, статистика

## ⏱️ Тайминг (5 минут = 300 секунд):
- Слайд 1-2: 45 сек (введение)
- Слайд 3-4: 60 сек (технологии + БД)
- Слайд 5-7: 90 сек (код - ГЛАВНОЕ)
- Слайд 8-9: 60 сек (инфраструктура)
- Слайд 10-12: 45 сек (результаты + заключение)

## 🎨 Оформление:
- Минимум текста (код + пункты)
- Схемы и диаграммы
- Скриншоты интерфейса
- Контрастные цвета для кода
- Единый стиль

## 💡 Советы:
- Говорите уверенно про КОД
- Покажите сложные моменты (OAuth, AJAX)
- Держите IDE открытой для демо
- Подготовьте запущенный Docker
- Репетируйте с таймером

---

**Удачи на защите! 🚀**

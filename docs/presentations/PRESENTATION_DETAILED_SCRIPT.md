# 📖 Подробный скрипт презентации MoviePortal
## Полный текст для выступления по структуре проекта

---

## 🎯 СЛАЙД 1: Титульный слайд + Цели и задачи (45 секунд)

### Что говорить:

**Приветствие и представление:**
> "Добрый день, уважаемая комиссия! Меня зовут [ФИО], я учусь в группе [номер группы]. Сегодня я представляю курсовой проект **MoviePortal** - веб-приложение для каталогизации фильмов."

**Цель проекта:**
> "**Цель** - разработать полнофункциональное веб-приложение для каталогизации фильмов с административной панелью и современной системой авторизации."

**Задачи (перечислить кратко):**
> "Для достижения цели были решены следующие задачи:"
1. **Создать дизайн** - разработать концепцию, цветовую схему и UI компоненты
2. **Сверстать макет** - реализовать адаптивную верстку на HTML/CSS/JS
3. **Настроить окружение** - развернуть Docker-инфраструктуру с тремя контейнерами
4. **Реализовать работу с БД** - создать запросы на получение, добавление и обновление данных
5. **Реализовать авторизацию** - интегрировать OAuth через Яндекс ID

**Что на слайде:**
- Название: MoviePortal
- Цель проекта
- 5 основных задач списком
- ФИО, группа, дата

---

## 🎯 СЛАЙД 2: Как создавался дизайн (90 секунд)

### Что говорить:

## 🎯 СЛАЙД 2: Как создавался дизайн (90 секунд)

### Что говорить:

**Введение:**
> "Первый этап - это **создание дизайна**. Я начал с выбора концепции и подхода к визуальному оформлению."

### **1. Концепция дизайна**

**Объяснение:**
> "Я выбрал **минималистичный подход** с акцентом на контент - фильмы должны быть в центре внимания, а не перегруженный интерфейс."

> "Использована **темная цветовая схема** - это современный тренд в дизайне, который создает эффект кинотеатра и снижает нагрузку на глаза при длительном просмотре."

> "Принят подход **mobile-first** - сначала проектировалась мобильная версия, затем расширялась для планшетов и десктопов."

### **2. Цветовая палитра**

**Показать код CSS:**
```css
--primary-color: #1a1a2e;      /* Основной темный фон */
--secondary-color: #16213e;     /* Карточки и блоки */
--accent-color: #0f3460;        /* Элементы навигации */
--highlight-color: #e94560;     /* Кнопки и акценты */
--text-color: #eaeaea;          /* Текст */
```

**Объяснение:**
> "Палитра состоит из пяти основных цветов. Темно-синие оттенки создают глубину, а **ярко-розовый highlight-color** используется для важных элементов - кнопок, ссылок, hover-эффектов. Это создает контраст и направляет внимание пользователя."

### **3. UI компоненты**

**Перечислить:**
> "При проектировании интерфейса были разработаны ключевые компоненты:"

1. **Карточки фильмов** с hover-эффектами - при наведении карточка приподнимается и появляется тень
2. **Навигационное меню** с бургер-иконкой для мобильных устройств
3. **Поиск в реальном времени** с выпадающим списком результатов
4. **Модальные окна** для OAuth авторизации

### **4. Инструменты и подход**

**Объяснение:**
> "Для layout использовал современные технологии: **CSS Grid** для сеток фильмов и **Flexbox** для выравнивания элементов внутри карточек."

> "Определены **breakpoints** - точки перелома адаптивности на 768px для планшетов и 1024px для десктопов."

**Переход:**
> "После создания дизайн-концепции началась верстка макета."

---

## 🎯 СЛАЙД 3: Как верстался макет (120 секунд)

### Что говорить:

**Введение:**
> "Второй этап - **верстка макета**. Здесь задача была превратить дизайн-концепцию в рабочий код."

### **1. Технологии верстки**

**Перечислить:**
> "Использован классический стек фронтенд-технологий:"
- **HTML5** с семантическими тегами - `<nav>`, `<article>`, `<section>`, `<header>`, `<footer>`
- **CSS3** с современными возможностями - Grid, Flexbox, переменные, анимации
- **Vanilla JavaScript** без фреймворков - для демонстрации глубокого понимания основ

### **2. Структура CSS**

**Показать файловую структуру:**
```
assets/css/
├── styles.css              # Основные стили, переменные, общие классы
├── films_style.css         # Страница каталога фильмов
├── film_page_style.css     # Детальная страница фильма
├── genres_style.css        # Страница жанров
├── directors_style.css     # Страница режиссеров
└── admin-styles.css        # Стили админ-панели
```

**Объяснение:**
> "Стили разбиты на отдельные файлы по страницам - это облегчает поддержку и позволяет загружать только нужные стили на конкретной странице."

### **КОД 1: Адаптивная сетка фильмов**

**Показать CSS:**
```css
.movie-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 20px;
    padding: 20px;
}

@media (max-width: 768px) {
    .movie-grid {
        grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
        gap: 15px;
    }
}
```

**Объяснение:**
> "Это ключевой CSS для отображения каталога фильмов. Использован **CSS Grid** с функцией `repeat(auto-fill, minmax())`."

> "Функция `auto-fill` автоматически рассчитывает, сколько карточек поместится в ряд. `minmax(200px, 1fr)` означает: минимальная ширина карточки 200 пикселей, максимальная - равная доля оставшегося пространства."

> "Для экранов меньше 768px через **медиа-запрос** карточки уменьшаются до 150px минимум, и gap между ними сокращается. Это обеспечивает комфортный просмотр на смартфонах."

### **КОД 2: Анимации и hover-эффекты**

**Показать CSS:**
```css
.movie-card {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.movie-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 20px rgba(233, 69, 96, 0.3);
}
```

**Объяснение:**
> "Для улучшения пользовательского опыта добавлены **плавные анимации**."

> "При наведении мыши на карточку фильма срабатывает псевдокласс `:hover`. Карточка **приподнимается** на 10 пикселей вверх через `translateY(-10px)` и под ней появляется **цветная тень** с розоватым оттенком."

> "Свойство `transition` делает это изменение плавным за 0.3 секунды. Функция `ease` создает естественное ускорение-замедление."

### **КОД 3: Мобильное меню**

**Показать JavaScript:**
```javascript
// Мобильное меню с hamburger-иконкой
menuToggle.addEventListener('click', () => {
    nav.classList.toggle('active');
    menuToggle.classList.toggle('active');
});
```

**Объяснение:**
> "На мобильных устройствах полное меню не помещается, поэтому используется **hamburger-меню**."

> "При клике на иконку три полоски срабатывает обработчик события. Метод `classList.toggle('active')` добавляет или убирает CSS класс 'active'."

> "Когда класс добавлен, меню выезжает сбоку через CSS transform, а иконка hamburger анимируется в крестик для закрытия."

### **4. Доступность (a11y)**

**Перечислить:**
> "При верстке учитывалась **доступность** для людей с ограниченными возможностями:"
- `aria-label` для навигационных элементов
- Семантические HTML теги вместо `<div>` везде
- `alt` атрибуты для всех изображений
- Контрастность цветов соответствует **WCAG AA**

**Переход:**
> "После верстки перешел к настройке окружения разработки."

---

## 🎯 СЛАЙД 4: Как настраивалось окружение (90 секунд)

### Что говорить:

**Введение:**
> "Третий этап - **настройка окружения разработки**. Здесь задача была создать изолированное, воспроизводимое окружение."

### **1. Docker-compose архитектура**

**Показать схему:**
```
┌─────────────┐
│   Nginx     │ ← Порт 80
│  (веб-сервер) │
└──────┬──────┘
       │
┌──────▼──────┐
│  PHP-FPM    │ ← PHP 8.2
│ (обработка)  │
└──────┬──────┘
       │
┌──────▼──────┐
│ PostgreSQL  │ ← База данных
│   (данные)   │
└─────────────┘
```

**Объяснение:**
> "Выбрана **контейнеризация** с помощью Docker. Инфраструктура состоит из **трех контейнеров**:"

1. **Nginx** - веб-сервер, принимает HTTP запросы на порту 80
2. **PHP-FPM** - обрабатывает PHP скрипты через FastCGI протокол
3. **PostgreSQL 12** - хранит все данные приложения

> "Все контейнеры находятся в одной Docker сети и общаются между собой по именам сервисов."

### **КОД 1: Dockerfile для PHP**

**Показать:**
```dockerfile
FROM php:8.2-apache
RUN apt-get update && apt-get install -y \
    libpq-dev \
    && docker-php-ext-install pdo pdo_pgsql pgsql
```

**Объяснение:**
> "Это Dockerfile для PHP контейнера. Берется **базовый образ** PHP 8.2 с Apache."

> "Затем устанавливаются библиотеки для работы с PostgreSQL - `libpq-dev`. И подключаются PHP расширения: **PDO** и **pdo_pgsql** для безопасной работы с БД."

### **КОД 2: docker-compose.yml**

**Показать:**
```yaml
services:
  nginx:
    build: ./docker/nginx
    ports: ["80:80"]
    depends_on: [php]
    
  php:
    build: ./docker/php
    volumes: ["./public:/var/www/html"]
    
  postgres:
    image: postgres:12
    environment:
      POSTGRES_DB: movies_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    volumes:
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql
```

**Объяснение:**
> "Файл docker-compose.yml **декларативно описывает** всю инфраструктуру."

> "Сервис **nginx** собирается из Dockerfile, пробрасывает порт 80 и зависит от PHP - значит PHP запустится первым."

> "Сервис **php** монтирует локальную папку public внутрь контейнера через `volumes`. Это значит изменения в коде **сразу видны** без пересборки."

> "Сервис **postgres** использует готовый образ PostgreSQL 12. Через `environment` задаются имя базы, пользователь и пароль. Также монтируется SQL файл в специальную папку - он **автоматически выполнится** при первом запуске для создания таблиц."

### **3. Запуск одной командой**

**Объяснение:**
> "Главное преимущество Docker - запуск всей инфраструктуры **одной командой**:"

```bash
docker-compose up -d
```

> "Флаг `-d` означает detached mode - контейнеры запускаются в фоне. Через минуту приложение готово к работе на http://localhost."

**Переход:**
> "Теперь рассмотрим примеры работы с базой данных."

---

## 🎯 СЛАЙД 5: Примеры запросов к базе данных (150 секунд)

### Что говорить:

**Введение:**
> "Четвертый этап - **работа с базой данных**. Сейчас покажу три типа запросов: получение, добавление и обновление данных."

### **Пример 1: ПОЛУЧЕНИЕ данных (SELECT)**

**Показать код:**
```php
// films.php - получение списка фильмов с пагинацией
$page = isset($_GET['page']) ? max(1, (int)$_GET['page']) : 1;
$per_page = 5;
$offset = ($page - 1) * $per_page;

$stmt = $pdo->prepare("
    SELECT 
        m.id,
        m.title,
        m.year,
        m.rating,
        d.name AS director,
        STRING_AGG(g.name, ', ') AS genres
    FROM movies m
    JOIN directors d ON m.director_id = d.id
    JOIN movie_genres mg ON m.id = mg.movie_id
    JOIN genres g ON mg.genre_id = g.id
    GROUP BY m.id, m.title, m.year, m.rating, d.name
    ORDER BY m.year DESC
    LIMIT ? OFFSET ?
");
$stmt->execute([$per_page, $offset]);
$movies = $stmt->fetchAll(PDO::FETCH_ASSOC);
```

**Объяснение:**
> "Это запрос на получение списка фильмов из файла films.php с **пагинацией**."

> "Сначала вычисляется номер страницы из GET параметра с защитой через `max(1, ...)` - нельзя запросить страницу меньше первой. Количество фильмов на странице - 5, вычисляется смещение offset."

> "SQL запрос использует **три JOIN** для объединения таблиц:"
- JOIN с directors получает имя режиссёра
- JOIN с movie_genres и genres получает жанры фильма

> "Функция **STRING_AGG** агрегирует все жанры фильма в одну строку через запятую. Например: 'Драма, Триллер'."

> "`GROUP BY` группирует результаты по фильму, чтобы получить одну строку на фильм со всеми его жанрами."

> "`LIMIT ? OFFSET ?` обеспечивают **пагинацию** - берутся только 5 фильмов начиная с нужной позиции."

> "Плейсхолдеры `?` защищают от SQL-инъекций - значения передаются через `execute()`."

### **Пример 2: ДОБАВЛЕНИЕ данных (INSERT)**

**Показать код:**
```php
// admin/movies.php - добавление нового фильма
if ($_POST['action'] === 'add') {
    $title = trim($_POST['title']);
    $year = (int)$_POST['year'];
    $director_id = (int)$_POST['director_id'];
    $rating = floatval($_POST['rating']);
    
    // Начинаем транзакцию
    $pdo->beginTransaction();
    
    try {
        // Вставка фильма
        $stmt = $pdo->prepare("
            INSERT INTO movies (title, year, director_id, rating, poster_url) 
            VALUES (?, ?, ?, ?, ?)
        ");
        $stmt->execute([$title, $year, $director_id, $rating, $poster_url]);
        
        // Получаем ID добавленного фильма
        $movie_id = $pdo->lastInsertId();
        
        // Добавление жанров (many-to-many)
        if (!empty($_POST['genres'])) {
            $genre_stmt = $pdo->prepare("
                INSERT INTO movie_genres (movie_id, genre_id) 
                VALUES (?, ?)
            ");
            
            foreach ($_POST['genres'] as $genre_id) {
                $genre_stmt->execute([$movie_id, (int)$genre_id]);
            }
        }
        
        $pdo->commit();
        
    } catch (PDOException $e) {
        $pdo->rollBack();
        $error = "Ошибка: " . $e->getMessage();
    }
}
```

**Объяснение:**
> "Это добавление нового фильма через админ-панель. Здесь несколько важных моментов."

> "**Валидация входных данных**: `trim()` удаляет пробелы, `(int)` и `floatval()` приводят к нужным типам - это базовая защита от некорректных данных."

> "**Транзакция** начинается с `beginTransaction()`. Это критически важно, потому что нужно выполнить несколько связанных операций: добавить фильм И связать его с жанрами."

> "Если хотя бы одна операция упадет с ошибкой, транзакция откатится через `rollBack()` - ничего не сохранится в базе. Это обеспечивает **целостность данных**."

> "После вставки фильма получаем его ID через `lastInsertId()` - это автоинкрементное значение, созданное базой."

> "Затем в цикле добавляются связи фильма с жанрами в таблицу movie_genres. Это реализация **many-to-many** связи."

> "В конце `commit()` фиксирует все изменения. Если всё прошло успешно - данные сохранены, если нет - откатятся."

### **Пример 3: ОБНОВЛЕНИЕ данных (UPDATE)**

**Показать код:**
```php
// admin/movies.php - редактирование фильма
if ($_POST['action'] === 'edit') {
    $id = (int)$_POST['id'];
    $title = trim($_POST['title']);
    $year = (int)$_POST['year'];
    $rating = floatval($_POST['rating']);
    
    $pdo->beginTransaction();
    
    try {
        // Обновление основной информации
        $stmt = $pdo->prepare("
            UPDATE movies 
            SET title = ?, year = ?, director_id = ?, rating = ?
            WHERE id = ?
        ");
        $stmt->execute([$title, $year, $director_id, $rating, $id]);
        
        // Удаление старых связей с жанрами
        $delete_stmt = $pdo->prepare("DELETE FROM movie_genres WHERE movie_id = ?");
        $delete_stmt->execute([$id]);
        
        // Добавление новых связей
        if (!empty($_POST['genres'])) {
            $genre_stmt = $pdo->prepare("
                INSERT INTO movie_genres (movie_id, genre_id) VALUES (?, ?)
            ");
            foreach ($_POST['genres'] as $genre_id) {
                $genre_stmt->execute([$id, (int)$genre_id]);
            }
        }
        
        $pdo->commit();
        
    } catch (PDOException $e) {
        $pdo->rollBack();
        $error = "Ошибка: " . $e->getMessage();
    }
}
```

**Объяснение:**
> "Редактирование фильма - это комбинация операций UPDATE и INSERT."

> "Сначала обновляется основная информация в таблице movies через **UPDATE**. Условие `WHERE id = ?` гарантирует, что обновится только нужный фильм."

> "Затем **удаляются все старые связи** фильма с жанрами через DELETE. Это проще, чем сравнивать, какие жанры добавились, какие удалились."

> "После удаления **добавляются новые связи** - просто вставляются заново все выбранные жанры."

> "Всё это в транзакции - либо обновится всё вместе, либо ничего не изменится."

**Итог по запросам:**
> "Как видите, все три типа операций - **SELECT, INSERT, UPDATE** - используют prepared statements для безопасности и транзакции для целостности данных."

**Переход:**
> "Теперь рассмотрим, как работает авторизация."

---

## 🎯 СЛАЙД 6: Как работает авторизация (120 секунд)

## 🎯 СЛАЙД 6: Как работает авторизация (120 секунд)

### Что говорить:

**Введение:**
> "Пятый этап - **реализация авторизации**. В проекте используются два типа авторизации: классическая для админов и OAuth для пользователей."

### **1. OAuth авторизация - схема работы (на примере Яндекс)**

**Показать схему потока:**
```
[1] Пользователь → "Войти через Яндекс"
         ↓
[2] Редирект на → https://oauth.yandex.ru/authorize
         ↓         (+ client_id, redirect_uri, state)
[3] Яндекс → Форма входа и подтверждения доступа
         ↓
[4] Редирект на → http://localhost/admin/yandex_callback.php
         ↓         (+ code, state)
[5] Сервер → POST к https://oauth.yandex.ru/token
         ↓   (обмен code на access_token)
[6] Яндекс → { "access_token": "...", "expires_in": 31536000 }
         ↓
[7] Сервер → GET к https://login.yandex.ru/info
         ↓   (Header: Authorization: OAuth token)
[8] Яндекс → { "id": "...", "login": "...", "first_name": "..." }
         ↓
[9] Проверка в БД → Поиск по yandex_id
         ↓
[10] Если найден → Авторизация + сохранение в $_SESSION
         ↓
[11] Если нет → Страница привязки аккаунта
```

**Объяснение шагов подробно:**

> "Яндекс использует стандартный **OAuth 2.0 Authorization Code Flow**. Это самый безопасный способ авторизации. Разберем каждый шаг:"

**Шаги 1-3: Инициация авторизации**
> "**Шаг 1-2**: Пользователь кликает на кнопку, и мы редиректим его на `oauth.yandex.ru/authorize` с параметрами:"
- `client_id` - ID нашего приложения в Яндексе
- `redirect_uri` - куда вернуть пользователя
- `state` - случайная строка для защиты от CSRF
- `scope` - какие данные запрашиваем (login:info)

> "**Шаг 3**: Яндекс показывает пользователю форму входа (если не залогинен) и запрос разрешения на доступ к данным."

**Шаги 4-6: Получение токена**
> "**Шаг 4**: После подтверждения Яндекс редиректит на наш callback с параметрами `code` (одноразовый код) и `state`."

> "**Шаг 5-6**: Наш сервер делает **серверный POST запрос** к Яндексу, передавая code, client_id и client_secret. Яндекс возвращает access_token. **Важно**: client_secret никогда не передается клиенту, только между серверами."

**Шаги 7-8: Получение данных**
> "**Шаг 7-8**: С полученным токеном делаем GET запрос к `/info` API. Яндекс возвращает данные пользователя."

**Шаги 9-11: Авторизация в системе**
> "**Шаг 9-10**: Ищем пользователя по yandex_id в БД. Если найден - авторизуем через сессию."

> "**Шаг 11**: Если не найден - показываем форму привязки, где пользователь вводит логин/пароль существующего админа."

### **КОД 1: Яндекс OAuth - получение токена**

**Показать код:**
```php
// admin/yandex_config.php
function getYandexAccessToken($code, $state) {
    // Проверка state для защиты от CSRF
    if (!isset($_SESSION['yandex_oauth_state']) || 
        $_SESSION['yandex_oauth_state'] !== $state) {
        throw new Exception('Invalid state parameter');
    }
    
    $params = [
        'grant_type' => 'authorization_code',
        'code' => $code,
        'client_id' => YANDEX_CLIENT_ID,
        'client_secret' => YANDEX_CLIENT_SECRET,
        'redirect_uri' => YANDEX_REDIRECT_URI
    ];
    
    // Отправка POST запроса к Яндекс для получения токена
    $ch = curl_init('https://oauth.yandex.ru/token');
    curl_setopt($ch, CURLOPT_POST, true);
    curl_setopt($ch, CURLOPT_POSTFIELDS, http_build_query($params));
    curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
    
    $response = curl_exec($ch);
    $data = json_decode($response, true);
    
    return $data['access_token'];
}
```

**Объяснение:**
> "Это функция получения токена доступа от Яндекс. Она демонстрирует классический OAuth 2.0 flow."

> "**Шаг 1 - Проверка state**: Параметр state - это случайная строка, которую мы генерируем перед редиректом на Яндекс и сохраняем в сессии. Когда Яндекс возвращает пользователя, мы проверяем, что state совпадает. Это **защита от CSRF атак** - если злоумышленник попытается подменить callback, state не совпадет."

> "**Шаг 2 - Формирование параметров**: Создаем массив с типом гранта 'authorization_code', кодом авторизации, client_id, client_secret и redirect_uri. Это стандартные параметры OAuth 2.0."

> "**Шаг 3 - CURL запрос**: Используем cURL для отправки POST запроса к API Яндекса на endpoint `/token`. Параметры кодируются как application/x-www-form-urlencoded."

> "**Шаг 4 - Получение токена**: Яндекс возвращает JSON с access_token, который мы используем для дальнейших API запросов."

### **КОД 2: Яндекс OAuth - получение данных пользователя**

**Показать код:**
```php
// admin/yandex_callback.php
// Получаем информацию о пользователе по токену
$user_data = getYandexUserInfo($access_token);

$yandex_id = $user_data['id'];
$login = $user_data['login'];
$first_name = $user_data['first_name'];
$avatar_url = "https://avatars.yandex.net/get-yapic/{$user_data['default_avatar_id']}/islands-200";

// Проверяем, есть ли пользователь в БД
$stmt = $pdo->prepare("SELECT id, username FROM admin_users WHERE yandex_id = ?");
$stmt->execute([$yandex_id]);
$admin_user = $stmt->fetch();

if ($admin_user) {
    // Авторизуем пользователя
    $_SESSION['admin_logged_in'] = true;
    $_SESSION['admin_id'] = $admin_user['id'];
    $_SESSION['yandex_authorized'] = true;
    
    // Обновляем last_login и профиль
    $update_stmt = $pdo->prepare("
        UPDATE admin_users 
        SET last_login = CURRENT_TIMESTAMP,
            yandex_avatar_url = ?,
            yandex_access_token = ?
        WHERE id = ?
    ");
    $update_stmt->execute([$avatar_url, $access_token, $admin_user['id']]);
    
    header('Location: index.php');
}
```

**Объяснение:**
> "После получения токена запрашиваем данные пользователя через API Яндекса."

> "**Функция getYandexUserInfo** делает GET запрос к `https://login.yandex.ru/info` с заголовком `Authorization: OAuth {token}`. Яндекс возвращает JSON с id пользователя, логином, именем, аватаром."

> "**Формирование URL аватара**: Яндекс возвращает только ID аватара, поэтому нужно сформировать полный URL по шаблону `https://avatars.yandex.net/get-yapic/{avatar_id}/islands-200`."

> "**Проверка в БД**: Ищем пользователя по `yandex_id`. Если найден - авторизуем. Если нет - предлагаем привязать Яндекс аккаунт к существующему админу."

> "Яндекс использует стандартный **OAuth 2.0 Authorization Code Flow** - это индустриальный стандарт авторизации с максимальной безопасностью. Обмен code на token происходит через серверный запрос, что гарантирует, что client_secret никогда не покидает сервер."

### **КОД 2: Проверка авторизации на защищенных страницах**

**Показать код:**
```php
// admin/auth.php - подключается в начале каждой админ-страницы
session_start();

// Проверка OAuth авторизации
    if (!isset($_SESSION['yandex_user']) && 
    
    // Не авторизован - редирект на страницу входа
    header('Location: login.php');
    exit();
}

// Проверка CSRF токена для форм
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    if (!isset($_POST['csrf_token']) || 
        $_POST['csrf_token'] !== $_SESSION['csrf_token']) {
        die('Неверный CSRF токен');
    }
}
```

**Объяснение:**
> "Это файл проверки авторизации, который подключается на каждой странице админ-панели."

> "**session_start()** инициирует или возобновляет сессию - получает данные пользователя из cookie."

> "Затем проверяется наличие **одного из двух** методов авторизации: Яндекс OAuth или классический вход по паролю."

> "Если ни один не найден - пользователь **не авторизован**, выполняется редирект на страницу входа, и скрипт немедленно останавливается через `exit()`."

> "Вторая проверка защищает от **CSRF атак** - Cross-Site Request Forgery. При отправке формы проверяется специальный токен."

> "CSRF токен генерируется случайно при создании формы и сохраняется в сессии. При submit-е формы токен должен совпадать. Если злоумышленник попытается отправить форму с другого сайта - токена не будет или он будет неверный."

### **3. Преимущества OAuth и особенности реализации**

**Перечислить:**
> "OAuth дает несколько важных преимуществ:"

1. **Удобство для пользователя** - не нужно создавать аккаунт и запоминать пароль
2. **Безопасность** - мы не храним пароли пользователей, риски при взломе минимальны
3. **Доверие** - пользователи доверяют крупному провайдеру Яндекс
4. **Быстрая регистрация** - авторизация в один клик
5. **Актуальные данные** - аватары и имя пользователя всегда актуальны

### **Реализованный OAuth провайдер - Yandex ID:**

**🟡 Yandex ID OAuth 2.0:**
> "Яндекс также использует **стандартный OAuth 2.0**, но с некоторыми особенностями:"

**Плюсы:**
- ✅ **Доверенный бренд** - Яндекс это крупнейший российский IT-гигант
- ✅ **Простое API** - минимальная настройка
- ✅ **Качественные аватары** через CDN avatars.yandex.net
- ✅ **Долгоживущие токены** - expires_in до 1 года
- ✅ **Стабильность** - редко падает, быстрый ответ API

**Минусы:**
- ❌ Меньшая аудитория чем VK
- ❌ Нужна регистрация приложения в Яндекс.OAuth

**Особенности реализации:**
```php
// 1. Редирект на авторизацию
https://oauth.yandex.ru/authorize?
  response_type=code&
  client_id=...&
  redirect_uri=...&
  state=... // CSRF защита

// 2. Обмен code на token (серверный запрос)
POST https://oauth.yandex.ru/token
Body: grant_type=authorization_code&code=...&client_id=...&client_secret=...

// 3. Получение данных пользователя
GET https://login.yandex.ru/info
Header: Authorization: OAuth {access_token}

// 4. Формирование URL аватара
$avatar = "https://avatars.yandex.net/get-yapic/{$avatar_id}/islands-200";
```

**Важная деталь Яндекс:** 
> "Яндекс возвращает только **ID аватара**, а не полный URL. Нужно формировать URL самостоятельно по шаблону. Размер можно выбрать: islands-small (28x28), islands-34, islands-middle (42x42), islands-50, islands-retina-small (56x56), islands-68, islands-75, islands-retina-middle (84x84), islands-retina-50 (100x100), islands-200."

---

### **Почему выбран Яндекс ID?**

> "Яндекс ID выбран в качестве OAuth провайдера по следующим причинам:"

1. **Стандартный протокол** - классический OAuth 2.0, демонстрирует понимание индустриальных стандартов
2. **Доверенный бренд** - Яндекс - крупнейший российский IT-гигант
3. **Широкая аудитория** - большинство российских пользователей имеют Яндекс аккаунт
4. **Простое и стабильное API** - хорошая документация, быстрый отклик

## 🎯 СЛАЙД 7: Выполнение по чеклисту (90 секунд)

### Что говорить:

**Введение:**
> "Важный момент - проект разрабатывался с учетом **чеклиста веб-разработки**. Сейчас покажу, что конкретно было реализовано."

### **Статистика выполнения**

**Показать цифры:**
```
✅ Выполнено:          15 пунктов (43%)
⚠️ Требует доработки:  12 пунктов (34%)
❓ Требует проверки:    8 пунктов (23%)

Итого: проверено 35 пунктов чеклиста
```

**Объяснение:**
> "По чеклисту было проверено **35 различных критериев** качества веб-приложения. Полностью выполнено 43%, что является хорошим показателем для учебного проекта."

### **Что СДЕЛАНО (выполнено полностью):**

**Категория 1: SEO и индексация**
> "**Первое направление - SEO оптимизация:**"
1. ✅ **robots.txt** - создан файл с правильными директивами для поисковых роботов
2. ✅ **sitemap.xml** - карта сайта со всеми страницами
3. ✅ **Meta-теги** - title, description, keywords на каждой странице
4. ✅ **Open Graph** - теги для красивых превью в соцсетях
5. ✅ **Семантические URI** - ЧПУ вместо index.php?id=123

**Категория 2: Безопасность**
> "**Второе - безопасность:**"
6. ✅ **Prepared Statements** - защита от SQL-инъекций через PDO
7. ✅ **CSRF защита** - токены в формах админ-панели
8. ✅ **Валидация данных** - проверка и фильтрация всех входных данных
9. ✅ **Session security** - безопасное хранение данных авторизации

**Категория 3: UX и доступность**
> "**Третье - пользовательский опыт:**"
10. ✅ **Адаптивный дизайн** - mobile-first верстка, работает на всех устройствах
11. ✅ **Страницы ошибок** - кастомные 404 и 403 страницы
12. ✅ **Пагинация** - постраничная разбивка больших списков
13. ✅ **Favicon** - иконка сайта в браузере
14. ✅ **Семантический HTML** - использование тегов nav, article, section
15. ✅ **Alt-теги для изображений** - доступность для screen readers

**Категория 4: Производительность**
> "**Четвертое - оптимизация:**"
16. ✅ **Gzip сжатие** - настроено в Nginx, экономит 70% трафика
17. ✅ **Кэширование статики** - CSS/JS/изображения кэшируются на год
18. ✅ **Минимизация запросов** - CSS объединен, нет лишних подключений

**Категория 5: Инфраструктура**
> "**Пятое - инфраструктура и развертывание:**"
19. ✅ **Docker контейнеризация** - воспроизводимое окружение
20. ✅ **Переменные окружения** - конфигурация без хардкода
21. ✅ **Автоматические миграции БД** - таблицы создаются автоматически

### **КОД: Примеры реализации**

**Показать robots.txt:**
```txt
User-agent: *
Allow: /
Disallow: /admin/
Disallow: /includes/
Disallow: /scripts/

Sitemap: https://your-domain.com/sitemap.xml
```

**Объяснение:**
> "В robots.txt разрешено индексировать весь сайт кроме служебных директорий - админки, includes, scripts. Указан путь к sitemap."

**Показать Open Graph:**
```html
<meta property="og:title" content="<?= htmlspecialchars($movie['title']) ?>">
<meta property="og:description" content="<?= htmlspecialchars($movie['description']) ?>">
<meta property="og:image" content="<?= $movie['poster_url'] ?>">
<meta property="og:type" content="video.movie">
```

**Объяснение:**
> "Open Graph теги обеспечивают красивые превью при репосте в соцсети. Указываются название, описание, постер и тип контента."

### **Что еще можно улучшить:**

**Перечислить кратко:**
> "Есть направления для дальнейшего развития:"
- Schema.org микроразметка для фильмов
- Автотесты PHPUnit
- Версия для печати через @media print
- CI/CD pipeline для автодеплоя

**Итог:**
> "Главное - проект соответствует **основным стандартам** современной веб-разработки: безопасность, производительность, SEO, доступность."

**Переход:**
> "Подведем итоги проделанной работы."

---

## 🎯 СЛАЙД 8: Результаты и демонстрация (60 секунд)

### Что говорить:

**Количественные результаты:**
> "Давайте подведем итоги в конкретных цифрах:"

**Код и файлы:**
- **Более 3000 строк кода**: PHP, JavaScript, CSS, SQL
- **15+ страниц**: главная, каталог, детальные страницы, админка
- **8 CSS файлов** для разных разделов
- **3 JS модуля**: поиск, загрузчики, скрипты

**База данных:**
- **4 основных таблицы**: movies, directors, genres, admin_users
- **2 связующие таблицы**: movie_genres (M:M), OAuth данные
- **18 фильмов** в тестовой базе
- **13 режиссеров**, **10 жанров**

**Интеграции:**
- **OAuth 2.0 авторизация**: Яндекс ID
- **REST API взаимодействие** с Яндекс сервисами
- **Стандартный Authorization Code Flow** с проверкой state

**Технологии:**
- **Docker**: 3 контейнера (Nginx, PHP, PostgreSQL)
- **PHP 8.2** с современным синтаксисом
- **PostgreSQL 12** с индексами и foreign keys
- **Vanilla JavaScript** - нет зависимостей от фреймворков

**Функционал:**
- **Поиск в реальном времени** - результаты < 0.5 сек
- **Полный CRUD** в админ-панели
- **Пагинация** списков
- **Адаптивный дизайн** 320px - 4K

**Соответствие стандартам:**
- **43% чеклиста** выполнено полностью
- **Prepared statements** во всех запросах
- **CSRF и XSS защита**
- **SEO оптимизация**: robots.txt, sitemap, meta-теги

### **Демонстрация (если есть время)**

**Вариант А: LIVE:**
> "Сейчас покажу работающее приложение..."
- Главная страница с каталогом
- Поиск в реальном времени
- Детальная страница фильма
- Админ-панель

**Вариант Б: Скриншоты:**
> "На слайде видны ключевые экраны приложения..."

**Переход:**
> "В заключение..."

---

## 🎯 СЛАЙД 9: Заключение (30 секунд)

### Что говорить:

**Достигнутые цели:**
> "Подводя итоги, **все поставленные задачи выполнены**:"

✅ **Дизайн создан** - минималистичная темная тема, адаптивные компоненты  
✅ **Макет сверстан** - HTML5/CSS3/JS, Grid и Flexbox, анимации  
✅ **Окружение настроено** - Docker с тремя контейнерами, одна команда запуска  
✅ **Запросы к БД реализованы** - SELECT, INSERT, UPDATE с prepared statements  
✅ **Авторизация работает** - OAuth 2.0 через Яндекс ID с проверкой state и серверным обменом токенов  
✅ **Чеклист выполнен** - 43% полностью, основные критерии соблюдены  

**Практическая ценность:**
> "Проект демонстрирует применение современного стека веб-технологий, может использоваться как основа для реального сервиса или учебный материал."

**Перспективы:**
> "Возможности для развития: REST API для мобильных приложений, рекомендательная система, интеграция с TMDB, система рецензий."

**Заключительная фраза:**
> "Благодарю за внимание! Готов ответить на ваши вопросы."

---

---

## 🎯 TIMING И СТРУКТУРА ПРЕЗЕНТАЦИИ

### Хронометраж по слайдам:
- **Слайд 1**: Титульный + Цели и задачи — **45 сек**
- **Слайд 2**: Как создавался дизайн — **90 сек** ⭐
- **Слайд 3**: Как верстался макет — **120 сек** ⭐
- **Слайд 4**: Как настраивалось окружение — **90 сек** ⭐
- **Слайд 5**: Примеры запросов к БД — **150 сек** ⭐⭐⭐ КРИТИЧНО
- **Слайд 6**: Как работает авторизация — **120 сек** ⭐⭐ ВАЖНО
- **Слайд 7**: Выполнение по чеклисту — **90 сек** ⭐
- **Слайд 8**: Результаты и демонстрация — **60 сек**
- **Слайд 9**: Заключение — **30 сек**

**ИТОГО**: ~12 минут  
**Для 5 минут**: сократить детали на слайдах 2-4, 7, оставить суть

### Что ОБЯЗАТЕЛЬНО показать:
✅ Слайд 5 - примеры запросов (SELECT, INSERT, UPDATE)  
✅ Слайд 6 - OAuth авторизация с проверкой подписи  
✅ Слайд 7 - что сделано по чеклисту  

### Что можно сократить:
- Слайд 2: только концепция и палитра (30 сек)
- Слайд 3: только адаптивная сетка (45 сек)
- Слайд 4: только docker-compose (45 сек)
- Слайд 7: только цифры статистики (30 сек)

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ ОТВЕТЫ НА ВОЗМОЖНЫЕ ВОПРОСЫ

### Вопрос 1: "Почему вы не использовали Laravel или другой PHP фреймворк?"

**Ответ:**
> "Отличный вопрос! Решение использовать чистый PHP было осознанным."

> "**Во-первых**, это демонстрирует глубокое понимание основ PHP. Фреймворки абстрагируют детали - роутинг, ORM, миграции. Когда пишешь с нуля, понимаешь, как это работает под капотом."

> "**Во-вторых**, полный контроль над архитектурой. Нет избыточного функционала. Приложение легковесное - весь код меньше мегабайта."

> "**В-третьих**, это учебный проект, цель - показать навыки программирования, а не умение пользоваться фреймворком."

> "Однако в production для крупных проектов я бы использовал Laravel для ускорения разработки."

---

### Вопрос 2: "Как вы защищаетесь от SQL-инъекций?"

**Ответ:**
> "Защита от SQL-инъекций - критически важный аспект."

> "**Главная защита** - PDO Prepared Statements во всех запросах. SQL запрос и данные передаются на сервер БД отдельно. База компилирует запрос, создает план, и только потом подставляет параметры."

> "Параметры всегда интерпретируются как данные, а не SQL код. Даже если передать `'; DROP TABLE movies; --`, это будет обычная строка для поиска."

> "**Дополнительно**: валидация данных - интовые значения через `(int)`, строки через `trim()`, регулярные выражения."

> "**Настройка БД**: принцип минимальных привилегий - PHP работает от пользователя с правами только SELECT, INSERT, UPDATE, DELETE, но нет DROP TABLE, ALTER."

---

### Вопрос 3: "Почему выбрали именно Яндекс ID для OAuth?"

**Ответ:**
> "Отличный вопрос! Выбор Яндекс ID был осознанным и имел несколько причин."

**Стандартный протокол:**
> "Яндекс использует **классический OAuth 2.0 Authorization Code Flow** - это индустриальный стандарт безопасной авторизации. Реализация такого протокола демонстрирует понимание современных практик веб-безопасности."

**Преимущества для пользователей:**
> "**Широкая аудитория** - большинство российских пользователей имеют аккаунт Яндекса (почта, навигатор, музыка, диск)."

> "**Доверенный бренд** - Яндекс это крупнейший российский IT-гигант, пользователи ему доверяют."

> "**Удобство** - не нужно создавать новый аккаунт и запоминать пароль."

**Технические преимущества:**
> "**Простое и стабильное API** - хорошая документация на русском языке, быстрый отклик серверов, редко падает."

> "**Качественные аватары** - Яндекс хранит аватары в CDN avatars.yandex.net с разными размерами."

> "**Долгоживущие токены** - expires_in до 1 года, не нужно часто обновлять."

**Для проекта:**
> "Это демонстрирует умение работать с REST API, OAuth 2.0, криптографическими проверками (state), серверным обменом токенов - всё то, что требуется в реальных проектах."

---

### Вопрос 4: "Как работает Яндекс OAuth подробнее?"

**Ответ:**
> "Давайте разберу на примере **Яндекс OAuth** - это классический OAuth 2.0."

**Этап 1: Регистрация приложения**
> "Сначала нужно зарегистрировать приложение на oauth.yandex.ru. Яндекс выдает client_id и client_secret - это учетные данные нашего приложения."

**Этап 2: Authorization Request**
> "Когда пользователь кликает 'Войти через Яндекс', мы редиректим его на:"

```
https://oauth.yandex.ru/authorize?
  response_type=code&
  client_id=abc123&
  redirect_uri=http://localhost/admin/yandex_callback.php&
  scope=login:info&
  state=random_string_12345
```

> "Параметр `state` - это случайная строка, которую мы генерируем и сохраняем в сессии. Это защита от CSRF."

**Этап 3: User Consent**
> "Яндекс показывает форму входа (если не залогинен) и **запрос разрешения**: 'Приложение MoviePortal хочет получить доступ к вашему имени и email. Разрешить?'"

**Этап 4: Authorization Code**
> "Если пользователь подтверждает, Яндекс редиректит обратно:"

```
http://localhost/admin/yandex_callback.php?
  code=7a8b9c0d&
  state=random_string_12345
```

> "`code` - это **одноразовый код**, действует 10 минут. Его нельзя использовать для API запросов."

**Этап 5: Token Exchange (критический момент!)**
> "Теперь наш **сервер** (не браузер!) делает POST запрос:"

```php
POST https://oauth.yandex.ru/token
Content-Type: application/x-www-form-urlencoded

grant_type=authorization_code&
code=7a8b9c0d&
client_id=abc123&
client_secret=SECRET&
redirect_uri=http://localhost/admin/yandex_callback.php
```

> "Яндекс проверяет: code валиден? client_id и client_secret совпадают? redirect_uri тот же?"

> "Если всё ок, возвращает JSON:"

```json
{
  "access_token": "AgAAAAA...",
  "expires_in": 31536000,
  "token_type": "bearer"
}
```

**Этап 6: API Requests**
> "Теперь с этим токеном можем делать API запросы:"

```php
GET https://login.yandex.ru/info
Authorization: OAuth AgAAAAA...
```

> "Яндекс возвращает данные пользователя: id, login, email, имя, аватар."

**Этап 7: Сохранение в БД**
> "Ищем пользователя по yandex_id в нашей БД. Если найден - авторизуем через $_SESSION. Если нет - предлагаем привязать к существующему админу."

**Почему это безопасно?**
1. **client_secret** никогда не передается клиенту
2. **code** одноразовый, живет 10 минут
3. **state** защищает от CSRF
4. **access_token** живет на сервере
5. Все через **HTTPS** в production

---

### Вопрос 5: "Как работает OAuth авторизация подробнее?"

**Ответ:**
> "OAuth 2.0 - протокол делегированной авторизации. На примере Яндекс ID:"

> "**Шаг 1**: Пользователь нажимает 'Войти через Яндекс'"

> "**Шаг 2**: Открывается страница Яндекс, где пользователь подтверждает доступ к базовой информации"

> "**Шаг 3**: Яндекс редиректит на callback URL с временным кодом авторизации"

> "**Шаг 4**: Сервер обменивает этот код на access token, отправляя запрос к API Яндекса с client_id, client_secret и кодом"

> "**Шаг 5**: Получаем access token и запрашиваем данные пользователя через login.info API. Сохраняем в сессии - это и есть авторизация"

> "**Преимущества**: пользователю не нужен новый аккаунт, мне не нужно хранить пароли - меньше рисков при взломе."

---

### Вопрос 4: "Как обеспечивается масштабируемость?"

**Ответ:**
> "Масштабируемость заложена в архитектуре:"

> "**Горизонтальное масштабирование**: Docker позволяет запустить несколько PHP-FPM контейнеров за Nginx-балансировщиком."

> "**База данных**: PostgreSQL поддерживает репликацию master-slave. Основной сервер на запись, реплики на чтение. Читающих запросов в 10 раз больше - это дает прирост производительности."

> "**Кэширование**: можно добавить Redis для кэша запросов к БД и хранения сессий."

> "**CDN**: статику можно разместить на Cloudflare - раздача с серверов, близких к пользователю."

> "**Архитектура**: трехуровневая (Nginx - PHP - PostgreSQL) позволяет масштабировать каждый уровень независимо."

---

### Вопрос 5: "Что бы вы улучшили в проекте?"

**Ответ:**
> "Есть направления для развития:"

**Технические:**
1. **REST API** с JSON ответами для мобильного приложения
2. **Автотесты** PHPUnit для критичных функций, Selenium для UI
3. **Redis** для кэширования частых запросов
4. **Очереди** RabbitMQ для тяжелых операций

**Функциональные:**
5. **Рекомендательная система** на основе просмотров
6. **Интеграция с TMDB** для автополучения информации о фильмах
7. **Система рецензий** пользователей
8. **Расширенный поиск** с фильтрами

**UX:**
9. **PWA** с установкой на устройство и офлайн-режимом
10. **Переключение тем** светлая/темная

> "Эти улучшения превратили бы проект в production-ready сервис."

---

## ✅ ЧЕКЛИСТ ПЕРЕД ЗАЩИТОЙ

**За день до:**
- [ ] Проверить Docker: `docker-compose up -d`
- [ ] Открыть http://localhost - работает?
- [ ] Презентация готова (PDF или PPTX)
- [ ] Код чистый, с комментариями
- [ ] Репетиция с таймером

**Перед выступлением:**
- [ ] Ноутбук заряжен
- [ ] Docker запущен
- [ ] Браузер открыт на localhost
- [ ] Презентация открыта
- [ ] Таймер готов

**Что иметь наготове:**
- [ ] Файлы кода для демонстрации
- [ ] docker-compose.yml
- [ ] config.php (PDO)
- [ ] search.php (AJAX)
- [ ] yandex_callback.php (OAuth)

---

---

**УДАЧИ НА ЗАЩИТЕ! 🚀**

**Помните**: Вы знаете проект лучше всех. Говорите уверенно, показывайте код, объясняйте решения. Главное - продемонстрировать понимание того, что вы сделали!

---

### Вопрос 2: "Как вы защищаетесь от SQL-инъекций?"

**Ответ:**
> "Защита от SQL-инъекций - критически важный аспект безопасности, и в проекте реализовано несколько уровней защиты."

> "**Главная защита** - это использование **PDO Prepared Statements** во всех запросах без исключения. Суть в том, что SQL запрос и данные передаются на сервер БД отдельно. Сначала база данных компилирует запрос, создаёт план выполнения, и только потом подставляет параметры."

> "При этом параметры всегда интерпретируются как данные, а не как SQL код. Даже если злоумышленник передаст в параметре что-то типа `'; DROP TABLE movies; --`, это будет воспринято как обычная строка для поиска, а не как команда удаления таблицы."

> "**Дополнительно** на входе данных я применяю валидацию и приведение типов: интовые значения явно кастую через `(int)`, строки обрезаю через `trim()`, использую регулярные выражения для проверки формата."

> "Также важна настройка БД: используется **принцип минимальных привилегий** - PHP приложение работает от пользователя БД, у которого есть только необходимые права SELECT, INSERT, UPDATE, DELETE на конкретных таблицах, но нет прав на DROP TABLE, ALTER и другие опасные операции."

---

### Вопрос 3: "Как работает OAuth авторизация?"

**Ответ:**
> "OAuth 2.0 - это протокол делегированной авторизации. Давайте разберём на примере авторизации через Яндекс ID."

> "**Шаг 1**: Пользователь на моём сайте нажимает кнопку 'Войти через Яндекс'. Это обычная ссылка, которая ведёт на oauth.yandex.ru с параметрами client_id и redirect_uri."

> "**Шаг 2**: Открывается страница Яндекс, где пользователь вводит логин и пароль (если ещё не авторизован), и подтверждает, что разрешает моему сайту получить его базовую информацию - имя, email, аватар."

> "**Шаг 3**: После подтверждения Яндекс перенаправляет пользователя обратно на мой сайт на специальный callback URL, передавая в GET параметрах временный код авторизации."

> "**Шаг 4**: На стороне моего сервера я делаю POST запрос к API Яндекса, отправляя этот код, мой client_id, client_secret и redirect_uri. В ответ получаю access_token - это ключ для доступа к данным пользователя."

> "**Шаг 5**: С помощью access_token я делаю GET запрос к API login.info и получаю информацию о пользователе: id, логин, имя, email, аватар. Эти данные я сохраняю в сессии - это и есть авторизация. Теперь на всех страницах я могу проверить наличие сессии и определить, кто зашёл."

> "**Преимущества** такого подхода: пользователю не нужно создавать ещё один аккаунт и запоминать пароль, а мне не нужно хранить пароли пользователей, что снижает риски в случае взлома."

---

### Вопрос 4: "Как обеспечивается масштабируемость приложения?"

**Ответ:**
> "Масштабируемость закладывалась на этапе проектирования архитектуры."

> "**Горизонтальное масштабирование**: благодаря Docker, можно легко запустить несколько инстансов PHP-FPM контейнера за одним Nginx, который будет выполнять роль load balancer - распределять запросы между ними."

> "**База данных**: PostgreSQL поддерживает репликацию master-slave. Можно настроить основной сервер на запись, и несколько реплик на чтение. Поскольку в веб-приложениях читающих запросов обычно в 10 раз больше, чем пишущих, это значительно повышает производительность."

> "**Кэширование**: в текущей реализации используется кэширование статики в браузере и Nginx. Для дальнейшего масштабирования можно добавить **Redis** для кэширования результатов запросов к БД и хранения сессий. Это уберёт нагрузку с PostgreSQL."

> "**CDN**: статические файлы - CSS, JS, изображения - можно разместить на CDN типа Cloudflare. Тогда они будут раздаваться с серверов, географически близких к пользователю, что ускоряет загрузку."

> "**Профилирование**: для мониторинга узких мест можно интегрировать инструменты типа New Relic или собственные логи slow queries в PostgreSQL, чтобы выявлять медленные запросы и оптимизировать их."

> "**Архитектура**: текущая трёхуровневая архитектура (Nginx - PHP - PostgreSQL) позволяет масштабировать каждый уровень независимо, в зависимости от того, где возникает узкое горло."

---

### Вопрос 5: "Что бы вы улучшили в проекте?"

**Ответ:**
> "Несмотря на то, что основные цели достигнуты, всегда есть возможности для улучшения. Вот что я бы добавил при дальнейшей разработке:"

**Технические улучшения:**

1. **"REST API"**: Создать полноценное RESTful API с JSON ответами. Это позволит легко разработать мобильное приложение или SPA фронтенд на React/Vue.

2. **"Автотесты"**: Написать unit-тесты с PHPUnit для критичных функций - авторизации, работы с БД, валидации. И end-to-end тесты с Selenium для тестирования UI.

3. **"Кэш Redis"**: Добавить Redis для кэширования частых запросов, например, списка популярных фильмов или жанров. Это снизит нагрузку на PostgreSQL.

4. **"Очереди"**: Для тяжёлых операций типа генерации sitemap или отправки email уведомлений использовать очереди RabbitMQ или Redis Queue.

**Функциональные улучшения:**

5. **"Рекомендательная система"**: Анализировать просмотры пользователя и рекомендовать похожие фильмы на основе жанров, режиссёров, рейтингов.

6. **"Интеграция с TMDB"**: Подключить The Movie Database API для автоматического получения информации о фильмах - постеров, описаний, актёрского состава.

7. **"Система рецензий"**: Позволить пользователям оставлять свои отзывы и оценки фильмов.

8. **"Расширенный поиск"**: Добавить фильтры по году, жанру, рейтингу, режиссёру. Использовать полнотекстовый поиск PostgreSQL для поиска по описанию.

**UX улучшения:**

9. **"Progressive Web App"**: Превратить сайт в PWA с возможностью установки на устройство и работы офлайн.

10. **"Темы оформления"**: Добавить переключение между тёмной и светлой темой, учитывая предпочтения пользователя.

> "Эти улучшения превратили бы проект из учебного в полноценный production-ready сервис, готовый к использованию реальными пользователями."

---

## 🎯 TIMING И ЧЕКЛИСТ

### Рекомендуемое время на каждый слайд:
- Слайд 1: 20 сек
- Слайд 2: 40 сек
- Слайд 3: 60 сек
- Слайд 4: 90 сек ⚠️ ВАЖНО
- Слайд 5: 120 сек ⚠️ КРИТИЧНО
- Слайд 6: 120 сек ⚠️ КРИТИЧНО
- Слайд 7: 90 сек
- Слайд 8: 90 сек
- Слайд 9: 90 сек
- Слайд 10: 60 сек
- Слайд 11: 40 сек
- Слайд 12: 45 сек

**ИТОГО**: ~12 минут (если время ограничено 5 минутами, сокращайте детали, но НЕ убирайте слайды 4-6 с кодом!)

### Перед защитой проверьте:
- [ ] Docker запущен: `docker-compose up -d`
- [ ] Сайт открывается: http://localhost
- [ ] Презентация готова и открыта
- [ ] Код примеров подготовлен
- [ ] Таймер установлен
- [ ] Глубокий вдох - вы готовы! 🚀

---

**УДАЧИ НА ЗАЩИТЕ! ВЫ ВСЁ ЗНАЕТЕ, ПРОСТО РАССКАЖИТЕ ЭТО УВЕРЕННО!**
